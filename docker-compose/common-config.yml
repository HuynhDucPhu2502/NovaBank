services:
  network-deploy-service:
    networks:
      - novabank-net

  common-db-config:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
    deploy:
      resources:
        limits:
          memory: 300m
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    extends:
      service: network-deploy-service

  microservice-base-config:
    environment:
      SPRING_RABBITMQ_HOST: rabbit
    deploy:
      resources:
        limits:
          memory: 512m
    extends:
      service: network-deploy-service

  microservice-config-client:
    environment:
      SPRING_CONFIG_IMPORT: configserver:http://infra-server:8888/
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://infra-server:8888/eureka/
    deploy:
      resources:
        limits:
          memory: 700m
    extends:
      service: microservice-base-config